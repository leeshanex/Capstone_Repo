@model IEnumerable<Capstone_Proj.Models.FishingTrip>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TripPhoto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TripHistory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Guide)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TripPhoto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TripHistory)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Guide.GuideId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TripId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TripId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TripId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
